Random r = new Random();

int n = r.Next(50);
int posX;
int posY;

for (int i = 0; i <= n; i++)
  {
    posX = r.Next(500);
    posY = r.Next(500);
    Console.WriteLine("X :" + posX + " Y :" + posY);
  }
